import { FilterEtoilesPipe } from './filter-etoiles.pipe';

describe('FilterEtoilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEtoilesPipe();
    expect(pipe).toBeTruthy();
  });
});
